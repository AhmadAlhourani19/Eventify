# --- OS / Editor junk ---
.DS_Store
Thumbs.db
*.swp
*.swo
*.orig
.idea/
.vscode/
.vscode/*
!.vscode/extensions.json

# --- Node / package managers ---
**/node_modules/
**/.pnp
**/.pnp.js

# lockfiles (keep exactly one; comment out the one you DON’T use)
# yarn.lock
# pnpm-lock.yaml
# npm-shrinkwrap.json

# --- Env secrets ---
.env
.env.*
!.env.example
!.env.sample

# --- Logs & diagnostics ---
**/logs/
**/*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# --- Coverage & test outputs ---
coverage/
**/coverage/
.nyc_output/
**/.nyc_output/
jest-cache/
jest-html-reporters-*.html
test-results.xml

# --- Build artifacts ---
dist/
**/dist/
build/
**/build/
# React (CRA) build
frontend/build/
# TypeScript build cache
*.tsbuildinfo

# --- Caches ---
.tmp/
temp/
tmp/
.cache/
**/.cache/
.eslintcache
stylelintcache
parcel-cache/
vite.config.ts.timestamp-*
vite.config.js.timestamp-*

# --- Babel / Webpack ---
.cache-loader/
bower_components/
.ember-cli
.sass-cache/

# --- Storybook (if used) ---
.storybook-out/
out/
storybook-static/

# --- Docker (optional) ---
docker-data/
.docker/
**/.dockerignore

# --- Misc (optional) ---
# SQLite or local DB files (if any)
*.sqlite
*.sqlite3
*.db
*.db-journal

# --- OS X / Windows extra ---
.AppleDouble
.LSOverride
Icon?
Ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# --- Images generated during dev (optional) ---
*.tgz
*.zip

# --- Monorepo: keep these ignored anywhere ---
**/.env.local
**/.env.development.local
**/.env.test.local
**/.env.production.local
